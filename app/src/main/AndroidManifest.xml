<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.itb.digim"
    android:installLocation="preferExternal" >

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.EqualizeActivity"
            android:label="@string/title_activity_equalize"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".view.ChainCodeActivity"
            android:label="@string/title_activity_chain_code"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".view.SelectBasePictureActivity"
            android:label="@string/title_activity_select_base_picture" >
        </activity>
    </application>

</manifest>
